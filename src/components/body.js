import styled from "@emotion/styled";

const Body = styled.div`
    display: inline-block;
    width: calc(100% - 200px);
    overflow: auto;
    min-height: 100vh;
    max-height: 100vh;
    flex: 1;
`

export default Body