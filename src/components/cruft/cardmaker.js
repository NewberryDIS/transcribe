import React from 'react'
const Cardmaker = (props) => {
    console.log(props)
    // const cards = props.itemObj.map((i) => <div>{i.id}</div>)
    const cards = <div>adsfashgfghg</div>
    return <div>{cards}</div>
}
export default Cardmaker